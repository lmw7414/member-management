spring:
  profiles:
    active: local
    group:
      local:
      test:

---
spring:
  config:
    activate:
      on-profile: local

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/member_management?allowPublicKeyRetrieval=true&useSSL=false
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  jpa:
    hibernate:
      dialect: org.hibernate.dialect.MariaDB
      ddl-auto: create
    show-sql: true
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    generate-ddl: true
    open-in-view: false


---
spring:
  config:
    activate:
      on-profile: test


  datasource:
    url: jdbc:h2:mem:testdb;MODE=MariaDB;
    driver-class-name: org.h2.Driver
    username: sa
    password: